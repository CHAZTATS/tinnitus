diff --git a/node_modules/@capacitor-community/native-audio/android/src/main/java/com/getcapacitor/community/audio/AudioDispatcher.java b/node_modules/@capacitor-community/native-audio/android/src/main/java/com/getcapacitor/community/audio/AudioDispatcher.java
index 6ed5d39..9b746d6 100644
--- a/node_modules/@capacitor-community/native-audio/android/src/main/java/com/getcapacitor/community/audio/AudioDispatcher.java
+++ b/node_modules/@capacitor-community/native-audio/android/src/main/java/com/getcapacitor/community/audio/AudioDispatcher.java
@@ -91,7 +91,7 @@ public class AudioDispatcher
     }
 
     public void loop() throws Exception {
-        mediaPlayer.setLooping(true);
+        invokePlay(0.0, true);
     }
 
     public void unload() throws Exception {
@@ -146,7 +146,7 @@ public class AudioDispatcher
             if (playing) {
                 mediaPlayer.pause();
                 mediaPlayer.setLooping(loop);
-                mediaState = PENDING_PLAY;
+                mediaState = (loop ? PENDING_LOOP : PENDING_PLAY);
                 seek(time);
             } else {
                 if (mediaState == PREPARED) {
@@ -168,8 +168,9 @@ public class AudioDispatcher
     public void onSeekComplete(MediaPlayer mp) {
         if (mediaState == PENDING_PLAY || mediaState == PENDING_LOOP) {
             Log.w("AudioDispatcher", "play " + mediaState);
+            boolean shouldLoop = mediaState == PENDING_LOOP;
             mediaPlayer.start();
-            mediaState = PLAYING;
+            mediaState = shouldLoop ? LOOPING : PLAYING;
         }
     }
 
diff --git a/node_modules/@capacitor-community/native-audio/android/src/main/java/com/getcapacitor/community/audio/NativeAudio.java b/node_modules/@capacitor-community/native-audio/android/src/main/java/com/getcapacitor/community/audio/NativeAudio.java
index 6e9938b..f5494c9 100644
--- a/node_modules/@capacitor-community/native-audio/android/src/main/java/com/getcapacitor/community/audio/NativeAudio.java
+++ b/node_modules/@capacitor-community/native-audio/android/src/main/java/com/getcapacitor/community/audio/NativeAudio.java
@@ -65,24 +65,7 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
     @Override
     protected void handleOnPause() {
         super.handleOnPause();
-
-        try {
-            if (audioAssetList != null) {
-                for (HashMap.Entry<String, AudioAsset> entry : audioAssetList.entrySet()) {
-                    AudioAsset audio = entry.getValue();
-
-                    if (audio != null) {
-                        boolean wasPlaying = audio.pause();
-
-                        if (wasPlaying) {
-                            resumeList.add(audio);
-                        }
-                    }
-                }
-            }
-        } catch (Exception ex) {
-            Log.d(TAG, "Exception caught while listening for handleOnPause: " + ex.getLocalizedMessage());
-        }
+        // Keep playback running across app background transitions.
     }
 
     @Override
